// Initial auth state
const initialState = {
  isAuthenticated: false,
  user: {}
};

// Auth actions
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
